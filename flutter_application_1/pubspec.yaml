name: flutter_application_1
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  url_launcher: ^6.1.5
  # diğer paketler burada listelenmeli

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
